# README additions - local frontend

To run the assistant demo frontend:

1. cd apps/assistant
2. npm install
3. npm run dev
4. Open http://localhost:3000

The frontend proxies to the local inference gateway at http://localhost:8081.
